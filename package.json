{
  "name": "agent-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate.ts",
    "check": "npm run format:check && npm run typecheck && npm run validate"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "oxfmt": "^0.58.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
